# Virtual Environment
venv/
.venv/
env/
.env/

# Python specific files
__pycache__/
*.pyc
*.pyo
*.pyd

# IDE specific files (e.g., VS Code)
.vscode/

# Data files that should not be in Git (if they are generated or sensitive)
# If your 'progress.json', 'syllabus.json', 'tasks.json' are *generated* by the app
# and shouldn't be part of the initial codebase, add them.
# If they are initial data, you can keep them.
data/progress.json
data/syllabus.json
data/tasks.json
# If you save ChromaDB embeddings locally
.chroma/
chroma_db_data/
chroma_data/

# Sensitive files (CRITICAL!)
credentials.json 
# Your image shows a 'credentials.json' file. This likely contains sensitive info like API keys. DO NOT PUSH!
# Any other local config files that might hold secrets
*.ini
*.log